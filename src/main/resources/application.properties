server.port=7070
server.host=0.0.0.0

# CORS
cors.allowedOrigins=http://localhost:3000,http://idoneo.localhost:3000,http://chicha.localhost:3000,http://tech.localhost:3000

# AWS
aws.region=us-east-1
aws.dynamodb.table=ProjectX
# aws.dynamodb.endpoint=http://localhost:8000

# Cognito (configurar con valores reales para producción)
# cognito.userPoolId=us-east-1_XXXXXXXXX
# cognito.clientId=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Auth mock para desarrollo (no requiere Cognito)
auth.mock=true

# Wompi — Configurar via variables de entorno en producción:
#   WOMPI_PUBLICKEY, WOMPI_PRIVATEKEY, WOMPI_INTEGRITYSECRET, WOMPI_EVENTSSECRET
# En desarrollo (auth.mock=true), se usa MockWompiPaymentService y no se necesitan llaves reales.
wompi.publicKey=
wompi.privateKey=
wompi.integritySecret=
wompi.eventsSecret=
wompi.apiBaseUrl=https://sandbox.wompi.co/v1

# SES (F009 — Email Notifications)
# ses.fromEmail=noreply@projectx.com
# email.enabled=false (true para usar SES real, false para LogEmailService)
